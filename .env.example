# Example environment variables for the project
# Copy this file to `.env` and replace the placeholders with your secrets.

# Required: your OpenAI API key (do NOT commit your actual key to the repo)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: override the model used by OpenAI
# OPENAI_MODEL=gpt-4o-mini
